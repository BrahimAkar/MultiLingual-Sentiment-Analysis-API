services:
  sentiment-api:
    build: .
    container_name: sentiment-api
    restart: unless-stopped
    networks:
      - local_network
    ports:
      - 8001:8001
networks:
  local_network:
    external: true
